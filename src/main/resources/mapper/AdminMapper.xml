<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zx.dao.AdminDao">

    <select id="login" parameterType="java.lang.String" resultType="int">
        SELECT id from admin  where adminname=#{usercode} and adminpassword=#{password}
    </select>


    <update id="out" parameterType="int">
        update user1 set qian=#{q} where id=1
    </update>

    <update id="in" parameterType="int">
        update user2 set qian=#{q} where id=1
    </update>

    <select id="qian1" resultType="int">
        select qian from user1 where id=1;
    </select>

    <select id="qian2" resultType="int">
        select qian from user2 where id=1;
    </select>

</mapper>
